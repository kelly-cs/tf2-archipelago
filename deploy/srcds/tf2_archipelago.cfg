// Archipelago integration.
//
// The bridge shares this container's network namespace, so it is on loopback
// and nothing outside the machine can reach it.
tf2ap_bridge_url "http://127.0.0.1:24680"

// Announce received items and cleared waves in chat. Errors are announced
// whatever this is set to.
tf2ap_announce "1"

// Show what the rest of the multiworld says in chat: hints, items other
// players found, anything typed with !apchat.
tf2ap_chat "1"

// Every bridge call and game event. 0 writes none, 1 writes them to the console
// and the SourceMod log, 2 writes them to the chat as well.
//
// 1 is the default because a debug bundle is only worth what it holds: the two
// files cost nothing to write, and asking a player to turn a convar on and hit
// the bug a second time usually ends the conversation.
tf2ap_debug "1"
