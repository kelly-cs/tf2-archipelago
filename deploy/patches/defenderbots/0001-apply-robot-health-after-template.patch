diff --git a/plugin/source/redbots3/generated/blu_assist.sp b/plugin/source/redbots3/generated/blu_assist.sp
--- a/plugin/source/redbots3/generated/blu_assist.sp
+++ b/plugin/source/redbots3/generated/blu_assist.sp
@@ -1,58 +1,37 @@
 /* Generated by internal/spbody from the Go it is named after. Do not edit. */
 
-#define BLU_ASSIST_FULL_TEAM (6.0)
-
 #define BLU_ASSIST_SAMPLE (25)
 
 int m_iBluAssistSeen;
+int m_iBluAssistMaxHealth[MAXPLAYERS + 1];
 ConVar redbots_manager_blu_health_scale;
+ConVar redbots_manager_blu_health_debug;
 
 stock void BluAssist_Init()
 {
 	m_iBluAssistSeen = 0;
-	redbots_manager_blu_health_scale = CreateConVar("sm_redbots_manager_blu_health_scale", "1.0", "What a robot's health is multiplied by when one human is on RED. Rises to 1.0 at six. 1.0 is off.", FCVAR_NOTIFY, true, 0.1, true, 1.0);
+	redbots_manager_blu_health_scale = CreateConVar("sm_redbots_manager_blu_health_scale", "1.0", "Direct multiplier applied to every BLU robot's health. 1.0 is neutral.", FCVAR_NOTIFY, true, 0.1, true, 10.0);
+	redbots_manager_blu_health_debug = CreateConVar("sm_redbots_manager_blu_health_debug", "0", "Log the original, requested and observed health for every scaled BLU robot.", FCVAR_NONE, true, 0.0, true, 1.0);
 }
 
-stock int BluAssist_HumansOnRed()
+stock float BluAssist_HealthScale()
 {
-	int count = 0;
-	for (int i = 1; i <= MaxClients; i++)
-	{
-		if (IsClientInGame(i) && !IsFakeClient(i) && !IsClientSourceTV(i) && (TF2_GetClientTeam(i) == TFTeam_Red))
-		{
-			count++;
-		}
-	}
-	return count;
+	return redbots_manager_blu_health_scale.FloatValue;
 }
 
-stock float BluAssistScale(ConVar convar)
+static Action BluAssistGetMaxHealth(int entity, int &maxHealth)
 {
-	float atOne = convar.FloatValue;
-	if (atOne >= 1.0)
-	{
-		return 1.0;
-	}
-	float humans = float(BluAssist_HumansOnRed());
-	if (humans < 1.0)
+	if (m_iBluAssistMaxHealth[entity] <= 0)
 	{
-		humans = 1.0;
+		return Plugin_Continue;
 	}
-	if (humans >= BLU_ASSIST_FULL_TEAM)
-	{
-		return 1.0;
-	}
-	return atOne + ((1.0 - atOne) * ((humans - 1.0) / 5.0));
-}
-
-stock float BluAssist_HealthScale()
-{
-	return BluAssistScale(redbots_manager_blu_health_scale);
+	maxHealth = m_iBluAssistMaxHealth[entity];
+	return Plugin_Changed;
 }
 
 stock void BluAssist_Describe(char[] buffer, int maxlength)
 {
-	if ((redbots_manager_blu_health_scale == null) || (redbots_manager_blu_health_scale.FloatValue >= 1.0))
+	if ((redbots_manager_blu_health_scale == null) || (FloatAbs(redbots_manager_blu_health_scale.FloatValue - 1.0) < 0.0001))
 	{
 		return;
 	}
@@ -61,62 +40,79 @@ stock void BluAssist_Describe(char[] buffer, int maxlength)
 
 stock void BluAssist_OnRobotSpawn(int client)
 {
-	if (BluAssist_HealthScale() >= 1.0)
+	SDKUnhook(client, SDKHook_GetMaxHealth, BluAssistGetMaxHealth);
+	m_iBluAssistMaxHealth[client] = 0;
+	if (FloatAbs(BluAssist_HealthScale() - 1.0) < 0.0001)
 	{
 		return;
 	}
-	RequestFrame(BluAssistApplyToRobot, GetClientUserId(client));
+	CreateTimer(0.10, BluAssistApplyToRobot, GetClientUserId(client), TIMER_FLAG_NO_MAPCHANGE);
 }
 
-public void BluAssistApplyToRobot(int userid)
+static Action BluAssistApplyToRobot(Handle timer, int userid)
 {
 	int client = GetClientOfUserId(userid);
 	if ((client <= 0) || !IsClientInGame(client) || !IsPlayerAlive(client))
 	{
-		return;
+		return Plugin_Stop;
 	}
 	if ((TF2_GetClientTeam(client) != TFTeam_Blue) || !IsFakeClient(client))
 	{
-		return;
+		return Plugin_Stop;
 	}
 	float health = BluAssist_HealthScale();
-	if (health < 1.0)
+	if (FloatAbs(health - 1.0) >= 0.0001)
 	{
-		int maxHealth = GetEntProp(client, Prop_Data, "m_iMaxHealth");
+		int maxHealth = TEMP_GetPlayerMaxHealth(client);
+		if (maxHealth < GetClientHealth(client))
+		{
+			maxHealth = GetClientHealth(client);
+		}
 		int wanted = RoundToCeil(float(maxHealth) * health);
 		if (wanted < 1)
 		{
 			wanted = 1;
 		}
-		BluAssistBendAttrib(client, "max health additive bonus", 0.0, float(wanted - maxHealth), 1.0);
+		m_iBluAssistMaxHealth[client] = wanted;
+		SDKHook(client, SDKHook_GetMaxHealth, BluAssistGetMaxHealth);
 		SetEntProp(client, Prop_Data, "m_iMaxHealth", wanted);
 		SetEntityHealth(client, wanted);
+		DataPack verify = new DataPack();
+		verify.WriteCell(userid);
+		verify.WriteCell(maxHealth);
+		verify.WriteCell(wanted);
+		verify.WriteFloat(health);
+		CreateTimer(0.10, BluAssistVerifyRobot, verify, TIMER_FLAG_NO_MAPCHANGE | TIMER_DATA_HNDL_CLOSE);
 	}
-	BluAssistSay(client, health);
+	return Plugin_Stop;
 }
 
-stock void BluAssistSay(int client, float health)
+static Action BluAssistVerifyRobot(Handle timer, DataPack pack)
 {
-	if (health >= 1.0)
-	{
-		return;
-	}
-	m_iBluAssistSeen++;
-	if ((m_iBluAssistSeen % BLU_ASSIST_SAMPLE) != 0)
+	pack.Reset();
+	int client = GetClientOfUserId(pack.ReadCell());
+	int original = pack.ReadCell();
+	int wanted = pack.ReadCell();
+	float scale = pack.ReadFloat();
+	if ((client <= 0) || !IsClientInGame(client) || !IsPlayerAlive(client) || (TF2_GetClientTeam(client) != TFTeam_Blue) || !IsFakeClient(client))
 	{
-		return;
+		return Plugin_Stop;
 	}
-	LogMessage("BluAssist: robot %d of the wave, health %d of %d, wanted x%.2f", m_iBluAssistSeen, GetClientHealth(client), GetEntProp(client, Prop_Data, "m_iMaxHealth"), health);
+	BluAssistSay(client, original, wanted, TEMP_GetPlayerMaxHealth(client), scale);
+	return Plugin_Stop;
 }
 
-stock void BluAssistBendAttrib(int client, const char[] name, float neutral, float delta, float scale = 1.0)
+stock void BluAssistSay(int client, int original, int wanted, int observed, float health)
 {
-	Address attrib = TF2Attrib_GetByName(client, name);
-	float current = neutral;
-	if (attrib != Address_Null)
+	if (FloatAbs(health - 1.0) < 0.0001)
 	{
-		current = TF2Attrib_GetValue(attrib);
+		return;
 	}
-	TF2Attrib_SetByName(client, name, (current * scale) + delta);
+	m_iBluAssistSeen++;
+	bool mismatch = (observed != wanted) || (GetClientHealth(client) != wanted);
+	if (!mismatch && !redbots_manager_blu_health_debug.BoolValue && ((m_iBluAssistSeen % BLU_ASSIST_SAMPLE) != 0))
+	{
+		return;
+	}
+	LogMessage("BluAssist: robot %d, original %d, wanted %d (x%.2f), observed health/max %d/%d%s", m_iBluAssistSeen, original, wanted, health, GetClientHealth(client), observed, mismatch ? " MISMATCH" : "");
 }
-
